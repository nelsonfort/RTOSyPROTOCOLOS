// Biblioteca para operar con strings a bajo nivel.
#ifndef STRINGMANIPULATION_H
#define STRINGMANIPULATION_H

char* integerToString(int value, char* result, int base);
char* floatToString(float value, char* result);

#endif /* STRINGMANIPULATION_H */
